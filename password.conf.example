# Put the sudo password on one non-comment line, without shell quoting.
# You may also use PASSWORD= followed by the literal password.
# Copy this template to password.conf and set its permissions to 600.
